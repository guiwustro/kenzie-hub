import styled from "styled-components";

export const FormGroupPasswordStyled = styled.div`
  width: 100%;
  .password-show__button {
    position: absolute;
    right: 12px;
    top: 12px;
    svg {
      fill: var(--color-gray-1);
    }
  }
`;
